name: vulv

services:
  client:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "127.0.0.1:8080:80"
    networks:
      - vulv-network

networks:
  vulv-network:
    external: true
